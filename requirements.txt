# rota principal (render aprovado — ControlNet)
fal-client>=0.5
# rota alternativa (opcional)
replicate>=1.0
# métrica de fidelidade geométrica
opencv-python-headless>=4.9
numpy>=1.26
# interface (app/streamlit_app.py)
streamlit>=1.38
# motores de ESTUDO/moodboard (opcionais — core/engines/estudo/)
google-genai>=0.3          # Gemini (Nano Banana)
openai>=1.40                # GPT Image (OpenAI). Grok usa urllib puro, sem SDK.
