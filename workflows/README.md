# workflows

Exporte do ComfyUI em **Save (API Format)** e salve como `flux_depth.json`.
Depois confira os ids dos nós em `core/engines/comfy_engine.py` (dict `NODE`) —
eles mudam conforme o grafo que você montou.
