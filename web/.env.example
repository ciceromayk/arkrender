# ARKITEKT web — copie para web/.env e preencha.
# Supabase → Settings → API: URL e a chave "anon public" (NUNCA a service_role aqui).

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# URL da api/ (uvicorn local: http://localhost:8000)
VITE_API_BASE_URL=http://localhost:8000
