# Copie para .streamlit/secrets.toml para rodar localmente (não versione o real).
# No Streamlit Community Cloud: App → Settings → Secrets, cole o mesmo conteúdo.

FAL_KEY = "sua-chave-aqui"

# opcional — motor ComfyUI grátis (self-hosted). Veja docs/comfyui_gratis.md.
# muda a cada sessão do Colab, então normalmente é mais prático colar direto
# na barra lateral do app do que fixar aqui.
# ARKITEKT_COMFY_URL = "https://xxxx.trycloudflare.com"
